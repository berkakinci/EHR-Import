# Private overrides (optional)
# Public config (client ID, redirect URI, providers) is in config.json

# Data directory for private files (tokens, database, raw API responses)
# Default: ../EHR Import Private/ (sibling directory)
# DATA_DIR=/path/to/your/private/data
